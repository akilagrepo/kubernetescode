pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo 'Code checked out successfully'
            }
        }

        stage('Build image') {
            steps {
                echo 'Build stage is running'
            }
        }
    }
}
